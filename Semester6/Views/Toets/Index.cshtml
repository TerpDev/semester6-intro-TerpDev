@model IEnumerable<Semester6.Models.Toets>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vak)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vak.VakId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ToetsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ToetsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ToetsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
